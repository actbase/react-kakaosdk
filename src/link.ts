import { IKakaoLink } from './types';

const createDefaultButton = () => {};
const sendDefault = () => {};
const createCustomButton = () => {};
const createScrapButton = () => {};
const sendScrap = () => {};
const uploadImage = () => {};
const scrapImage = () => {};
const deleteImage = () => {};

const Link: IKakaoLink = {
  createDefaultButton,
  sendDefault,
  createCustomButton,
  createScrapButton,
  sendScrap,
  uploadImage,
  scrapImage,
  deleteImage,
};

export default Link;
